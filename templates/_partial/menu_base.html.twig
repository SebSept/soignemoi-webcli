{% block menu %}
    <ul>
        {% if app.user %}
            <li>{{ app.user.userIdentifier }}</li>
        {% endif %}

        <li><a href="{{ path('app_home') }}">Accueil</a></li>

        {% block user_menu %}{% endblock %}

        {% if not app.user %}
            <li><a href="{{ path('app_login') }}">Identification</a></li>
        {% else %}
            <li><a href="{{ path('app_logout') }}">Déconnexion</a></li>
        {% endif %}
    </ul>
{% endblock %}
